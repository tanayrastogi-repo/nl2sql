[project]
name = "nl2sql"
version = "0.1.0"
description = "Simple tool to convert natural language to SQL queries."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mypy>=1.20.2",
    "ruff>=0.15.12",
]
